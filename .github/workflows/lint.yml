name: AGLint

on:
    push:
        branches-ignore:
            - master
    pull_request:
        branches:
            - "**"

jobs:
    lint:
        name: Run AGLint
        runs-on: ubuntu-latest
        permissions:
            contents: read
        steps:
            -   name: Check out to repository
                uses: actions/checkout@v6

            -   name: Set up pnpm
                uses: pnpm/setup@v1
                with:
                    version: 11
                    runtime: node@22
                    cache: true

            -   name: Install dependencies
                run: pnpm install

            -   name: Run AGLint
                run: pnpm run lint
